users
  id
  username
  password

wines
  id
  name
  grape
  vineyard
  vintage
  region
  price
  picture

users_wines
  id
  user_id
  wine_id




to-do:
  add necessary database routes
  see if you can grab the image url


  });
})).then(function() {
  for (var j = 0; finalWines.length < 4; j++) {
    if (chosenWines[j].Picture.length > 10) {
      finalWines.push(chosenWines[j])
    }
  }
}).then(function() {
  res.send(finalWines);
// console.log(chosenWines);
});
});
